
🍎 Sistema Simples de Controle de Estoque (Quitanda)

---

📌 Sobre o Projeto

Este projeto foi desenvolvido como atividade prática para compreender o funcionamento de **arrays em JavaScript** e sua aplicação na criação de um sistema simples de controle de estoque.

A proposta simula uma pequena quitanda em processo de transformação digital, onde o proprietário precisa de uma aplicação básica que funcione diretamente no navegador para controlar o estoque de frutas.

O sistema não utiliza banco de dados — os dados permanecem armazenados apenas enquanto a página estiver aberta.

---

🎯 Objetivos da Atividade

Compreender na prática:

* ✅ O que é um **array**
* ✅ Como armazenar múltiplos valores em uma única variável
* ✅ Como manipular arrays utilizando:

  * `push()`
  * `pop()`
  * `length`
  * `join()`
* ✅ Como atualizar elementos da página utilizando o **DOM**

---
 🛠️ Tecnologias Utilizadas

* **HTML5** – Estrutura da página
* **CSS3** – Estilização da interface
* **JavaScript** – Lógica do sistema e manipulação do array

---

⚙️ Funcionalidades do Sistema

O sistema possui três funcionalidades principais:

📋 Mostrar Frutas

Exibe todas as frutas armazenadas no array na tela.

 ➕ Adicionar Fruta

Permite inserir uma nova fruta no estoque utilizando o método:

```javascript
array.push()
```

➖ Remover Última

Remove a última fruta adicionada ao estoque utilizando:

```javascript
array.pop()
```

---

 🧠 Conceitos Aplicados

 Array

Estrutura de dados que permite armazenar vários valores dentro de uma única variável.

Exemplo:

```javascript
let frutas = ["Maçã", "Banana", "Laranja"];
```

---

🔹 push()

Adiciona um novo elemento ao final do array.

---

🔹 pop()

Remove o último elemento do array.

---

🔹length

Retorna a quantidade de elementos existentes no array.

---

🔹 join()

Transforma os elementos do array em uma única string para exibição na tela.

🔹Exemplo:

```javascript
frutas.join(", ");
```

---

🔹Manipulação do DOM

O sistema utiliza JavaScript para atualizar dinamicamente o conteúdo da página, garantindo que as alterações no array sejam refletidas imediatamente na interface.

---

🚀 Como Executar o Projeto

1. Abra o VS Code (ou outro editor).
2. Crie um arquivo chamado:

   ```
   array_frutas.html
   ```
3. Cole o código fornecido pelo professor.
4. Salve o arquivo.
5. Abra no navegador.
6. Teste os botões:

   * Mostrar Frutas
   * Adicionar Fruta
   * Remover Última

---

 📊 Estrutura do Projeto

```
📁 projeto-estoque-frutas
 ├── array_frutas.html
 ├── style.css (se separado)
 └── README.md
```

---

🎓 Aprendizados

Durante o desenvolvimento deste projeto foi possível:

* Entender como arrays funcionam na prática
* Manipular dados dinamicamente
* Atualizar a interface utilizando JavaScript
* Trabalhar lógica de programação aplicada a uma situação real


---

 👩‍💻 Autora

**Paolla Paula Veronez**



---

